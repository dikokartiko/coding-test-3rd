/**
 * Chat Components Module
 *
 * This module exports the main components for the chat interface:
 * - ChatSidebar: Displays conversation history and allows switching between conversations
 * - ChatMain: Contains the main chat interface with header, message display, and input
 *
 * These components are designed to work together to create a complete chat experience
 * with sidebar navigation and main content area.
 */

/**
 * Export the ChatSidebar component for use in chat layouts
 * The sidebar contains conversation history and controls for managing conversations
 */
export { ChatSidebar } from "./chat-sidebar";

/**
 * Export the ChatMain component for use in chat layouts
 * The main component contains the header, fund selectors, message display, and input form
 */
export { ChatMain } from "./chat-main";
