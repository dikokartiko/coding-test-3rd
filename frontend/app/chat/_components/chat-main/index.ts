/**
 * Chat Main Component Module
 *
 * This module exports the main chat interface component and its associated types.
 * The ChatMain component serves as the primary container for the chat interface,
 * organizing the different sections of the chat UI into a cohesive layout.
 */

/**
 * Export the ChatMain component for use in chat layouts
 * The main component contains the header, fund selectors, message display, and input form
 */
export { ChatMain } from "./ChatMain";

/**
 * Export the ChatMainProps type definition
 * This type defines the interface for the properties that the ChatMain component expects
 */
export type { ChatMainProps } from "./chat-main.types";
