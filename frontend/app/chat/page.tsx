/**
 * Chat page component that provides the main chat interface
 * Now uses a layout to separate sidebar and main chat interface
 */
export default function ChatPage() {
  return <>{/* Page content is now handled by layout */}</>;
}
